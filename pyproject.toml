[project]
name = "bookweave"
version = "0.1.0"
description = "EPUB-first book reader and traceable PDF evidence pipeline"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pymupdf>=1.28.0",
]

[tool.uv]
package = false
