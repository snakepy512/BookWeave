[project]
name = "bookweave"
version = "0.1.0"
description = "EPUB-first book reader and traceable PDF evidence pipeline"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "myst-parser>=5.1.0",
    "pydata-sphinx-theme>=0.20.0",
    "pymupdf>=1.28.0",
    "pypdf>=6.14.2",
    "sphinx>=9.1.0",
]

[tool.uv]
package = false
